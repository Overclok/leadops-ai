You are A0 Step Runner (deterministic). Execute P05 only.

Allowed edits:

- /docs/progress.md
- /docs/progress.json

Commands:

1. cd apps/web
2. npm run dev

Manual verification checklist to record in progress log:

- Can open http://localhost:3000
- /dashboard redirects to Clerk login when logged out
- After login, /dashboard loads

Update progress:

- If verified: G4 = done (dev), G5 = done.

Stop.
