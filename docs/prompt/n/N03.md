Execute N03 (Deterministic). Goal: add an idempotent n8n workflow sync tool in the repo.
feedback in Italian.

Allowed edits:
- /infra/n8n/scripts/n8n-sync.mjs (new, real code)
- /infra/n8n/README.md (document usage and safety)
- /docs/progress.md
- /docs/progress.json

Forbidden:
- Storing secrets in repo
- Importing/updating workflows on the real instance (no network calls in this step)
- Editing workflow JSON files unless explicitly required by spec (not in this step)

Requirements for n8n-sync.mjs:
- Node >= 18 compatible (we will run in the repo, not inside n8n container)
- Reads env at runtime:
  - N8N_BASE_URL (e.g. https://lead-ops-n8n...)
  - N8N_API_KEY
- Reads all JSON files from: /infra/n8n/workflows/*.json
- Enforces deterministic identity:
  - Use workflow "name" as primary key
  - Optional tag convention in name: "leadops:<slug>@<version>"
- Supports modes:
  - DRY_RUN (default): prints a sorted plan, exits 0
  - APPLY=true: performs create/update via n8n REST API
- Plan format MUST be stable (sorted by workflow name):
  - CREATE <name>
  - UPDATE <name>
  - SKIP <name>
- Uses n8n REST API:
  - GET /api/v1/workflows?limit=250
  - POST /api/v1/workflows (create)
  - PATCH /api/v1/workflows/{id} (update)
  - Uses header: X-N8N-API-KEY
- Safety checks:
  - Refuse to run APPLY unless APPLY=true explicitly
  - Refuse to run if N8N_BASE_URL or N8N_API_KEY missing
  - Never print secrets
  - If workflow JSON missing "name" -> error and exit non-zero

Update docs:
1) infra/n8n/README.md:
  - Add section "Deterministic provisioning via n8n-sync"
  - Show commands:
    - DRY_RUN: node infra/n8n/scripts/n8n-sync.mjs
    - APPLY: APPLY=true node infra/n8n/scripts/n8n-sync.mjs
  - Explain where to store secrets (secret manager), never in repo
  - Explain that credentials must be configured in n8n UI beforehand

Progress updates:
- Add remote gate RN4: "n8n sync tool exists" -> DONE with evidence path
- Recommend next step: N04 (execute DRY_RUN then APPLY, verify imported workflows)

Output in chat:
- Confirm files created/edited and where
- Confirm RN4 done and next = N04
Stop.
