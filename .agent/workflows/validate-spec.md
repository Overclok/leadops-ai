
# Workflow: validate-spec
- schema JSON valido
- enum coerenti (DB == enums.json)
- migrazione Supabase applicabile
- ingestion HMAC + idempotenza ok
- `npm ci` o `npm install` in apps/web
- test base passano
