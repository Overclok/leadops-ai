
# 10 — Data contracts

- L’unico formato di evento accettato è `event-schema.json`.
- Gli enum ufficiali sono in:
  - DB (Supabase enum)
  - `.agent/skills/metrics-engine/references/enums.json`
- Cambi breaking → versionare schema (`schema_version`).
